stages:
  - build
  - plan
  - deploy
  - k8s_deploy

variables:
  AWS_ROLE_ARN: arn:aws:iam::510674263883:role/GitLabPipelineRole
  AWS_REGION: ap-south-1

# Standardizes AWS OIDC Auth for all Terraform jobs
.aws_auth: &aws_auth
  id_tokens:
    GITLAB_OIDC_TOKEN:
      aud: https://gitlab.com
  before_script:
    - mkdir -p ~/.aws
    - |
      echo "[default]
      role_arn = ${AWS_ROLE_ARN}
      web_identity_token_file = /tmp/web_identity_token" > ~/.aws/config
    - echo "${GITLAB_OIDC_TOKEN}" > /tmp/web_identity_token
    - export AWS_CONFIG_FILE=~/.aws/config

build_images:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/base:latest .
    - docker tag $CI_REGISTRY_IMAGE/base:latest $CI_REGISTRY_IMAGE/worker:latest
    - docker push $CI_REGISTRY_IMAGE/worker:latest
    - docker tag $CI_REGISTRY_IMAGE/base:latest $CI_REGISTRY_IMAGE/api:latest
    - docker push $CI_REGISTRY_IMAGE/api:latest

plan_infra:
  <<: *aws_auth
  stage: plan
  image: registry.gitlab.com/gitlab-org/terraform-images/stable:latest
  variables:
    TF_STATE_NAME: chaos-prod
  script:
    - cd infra
    - gitlab-terraform init
    - gitlab-terraform plan
  artifacts:
    paths:
      - infra/plan.cache

apply_infra:
  <<: *aws_auth
  stage: deploy
  image: registry.gitlab.com/gitlab-org/terraform-images/stable:latest
  variables:
    TF_STATE_NAME: chaos-prod
  dependencies:
    - plan_infra
  script:
    - cd infra
    - gitlab-terraform init
    - gitlab-terraform apply
    - echo "EC2_PUBLIC_IP=$(gitlab-terraform output -raw ec2_public_ip)" > ../deploy.env
  artifacts:
    reports:
      dotenv: deploy.env

configure_networking:
  stage: deploy
  image: alpine:latest
  needs:
    - job: apply_infra
      artifacts: true
  before_script:
    - apk add --update openssh-client
    - chmod 400 "$SSH_PRIVATE_KEY" # GitLab already created the file for you!
    - echo "Captured IP $EC2_PUBLIC_IP"
  script:
    - if [ -z "$EC2_PUBLIC_IP" ]; then echo "IP capture failed!"; exit 1; fi
    - |
      ssh -i /tmp/id_rsa -o StrictHostKeyChecking=no ubuntu@$EC2_PUBLIC_IP << 'EOF'
        MINIKUBE_IP=$(minikube ip)
        sudo iptables -t nat -C PREROUTING -p tcp --dport 31379 -j DNAT --to-destination $MINIKUBE_IP:31379 2>/dev/null || \
          sudo iptables -t nat -A PREROUTING -p tcp --dport 31379 -j DNAT --to-destination $MINIKUBE_IP:31379
        sudo iptables -C FORWARD -p tcp -d $MINIKUBE_IP --dport 31379 -j ACCEPT 2>/dev/null || \
          sudo iptables -A FORWARD -p tcp -d $MINIKUBE_IP --dport 31379 -j ACCEPT
        if ! dpkg -l | grep -q iptables-persistent; then
          echo iptables-persistent iptables-persistent/autosave_v4 boolean true | sudo debconf-set-selections
          sudo apt-get update && sudo apt-get install -y iptables-persistent
        fi
        sudo netfilter-persistent save
      EOF

deploy_pods:
  stage: k8s_deploy
  image: alpine:latest
  needs:
    - job: apply_infra
      artifacts: true
    - job: configure_networking
  before_script:
    - apk add --update openssh-client
    - cat "$SSH_PRIVATE_KEY" | tr -d '\r' > /tmp/id_rsa
    - echo "" >> /tmp/id_rsa
    - chmod 400 /tmp/id_rsa

  script:
    - scp -i /tmp/id_rsa -o StrictHostKeyChecking=no -r k8s/ ubuntu@$EC2_PUBLIC_IP:/home/ubuntu/
    - |
      ssh -i /tmp/id_rsa -o StrictHostKeyChecking=no ubuntu@$EC2_PUBLIC_IP << 'EOF'
        if ! command -v helm &> /dev/null; then
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
        fi
        helm repo add kedacore https://kedacore.github.io/charts || true
        helm repo update
        helm upgrade --install keda kedacore/keda --namespace keda --create-namespace
        minikube kubectl -- apply -f /home/ubuntu/k8s/
        minikube kubectl -- rollout restart deployment chaos-worker
      EOF